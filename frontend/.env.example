# NullHaven — Environment Configuration
# Copy this file to .env and fill in your deployed contract addresses
# NEVER commit .env to git — it is already in .gitignore

# ─── Stellar Network ─────────────────────────────────────────────────────────
VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
VITE_RPC_URL=https://soroban-testnet.stellar.org
VITE_HORIZON_URL=https://horizon-testnet.stellar.org

# ─── Deployed Contract IDs (fill after stellar contract deploy) ───────────────
VITE_CONTRACT_POOL=
VITE_CONTRACT_VERIFIER=
VITE_CONTRACT_ASP=
VITE_CONTRACT_COMPLIANCE=
